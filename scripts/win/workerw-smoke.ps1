#Requires -Version 5.1
# Optional runtime check for WorkerW (needs interactive desktop session).
param()
Write-Host "workerw-smoke: compile with cargo features workerw on a Windows host, then call attach_to_workerw(hwnd)."
Write-Host "This script is a placeholder checklist - HWND attach is app-specific."
exit 0
