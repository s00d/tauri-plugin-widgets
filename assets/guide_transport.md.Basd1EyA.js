import{_ as s,o as e,c as i,a5 as a}from"./chunks/framework.Cx-mnQW-.js";const n="/tauri-plugin-widgets/illustrations/transport.jpg",u=JSON.parse('{"title":"Apple data transport","description":"","frontmatter":{"title":"Apple data transport"},"headers":[],"relativePath":"guide/transport.md","filePath":"guide/transport.md","lastUpdated":1785753275000}'),o={name:"guide/transport.md"};function p(d,t,r,l,h,c){return e(),i("div",null,[...t[0]||(t[0]=[a('<h1 id="apple-data-transport" tabindex="-1">Apple data transport <a class="header-anchor" href="#apple-data-transport" aria-label="Permalink to &quot;Apple data transport&quot;">​</a></h1><p>The host writes widget data through <strong>one</strong> transport you choose in config. You know your signing setup — do not rely on runtime fan-out.</p><div class="doc-illust"><p><img src="'+n+`" alt="Single transport channel from host app to widget surface"></p></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;plugins&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;widgets&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;appGroup&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;group.com.example.myapp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;transport&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;appGroup&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;extensionBundleId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;com.example.myapp.widgetkit&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Situation</th><th><code>transport</code></th></tr></thead><tbody><tr><td>Release / Team ID + App Groups enabled</td><td><code>appGroup</code></td></tr><tr><td>Mac App Store</td><td><code>appGroup</code></td></tr><tr><td>Local ad-hoc signing (no shared App Group container)</td><td><code>widgetContainer</code></td></tr><tr><td>iOS (device and simulator)</td><td><code>appGroup</code> only — other values fail at plugin init</td></tr><tr><td>Not sure yet</td><td><code>auto</code> once at startup (dev only) — read the log, then pin the winner in conf</td></tr></tbody></table><table tabindex="0"><thead><tr><th><code>transport</code></th><th>Host write path</th><th>Requirements</th></tr></thead><tbody><tr><td><code>appGroup</code></td><td><code>containerURL(group)/widget_data.json</code></td><td>Real Team ID + App Groups on App + Extension</td></tr><tr><td><code>userDefaults</code></td><td>App Group <code>UserDefaults</code> suite</td><td>Same as <code>appGroup</code></td></tr><tr><td><code>widgetContainer</code></td><td><code>~/Library/Containers/&lt;appex&gt;/Data/widget_data.json</code></td><td>macOS host <strong>not</strong> sandboxed; works with ad-hoc</td></tr><tr><td><code>auto</code></td><td>One-shot probe, then latch</td><td>Development only — never ship this</td></tr></tbody></table><p>Wrong <code>transport</code> / missing <code>appGroup</code> <strong>fails plugin init</strong> with a concrete message (empty widgets from a silent fallback are not a thing).</p><p><code>init-macos</code> / <code>init-ios</code> write this block (transport inferred on macOS from codesign identities; iOS always <code>appGroup</code>). Re-run discovery anytime:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tauri-widgets</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> signing</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tauri-widgets</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> signing</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --apply</span></span></code></pre></div><p>Override without editing conf: <code>WIDGET_TRANSPORT=widgetContainer</code>.</p><p>The <strong>widget extension</strong> still reads all channels and picks the freshest map (so it can find data wherever the host wrote). Host-side writes use only the configured driver. Render receipts feed <code>getWidgetDiagnostics</code>, not transport selection.</p><p><code>setItems</code> skips disk I/O when the value is unchanged (no nonce bump).</p><h2 id="app-groups-signing-plugin-consumers" tabindex="-1">App Groups &amp; signing (plugin consumers) <a class="header-anchor" href="#app-groups-signing-plugin-consumers" aria-label="Permalink to &quot;App Groups &amp; signing (plugin consumers)&quot;">​</a></h2><p>For live widgets that share data with the host you need the <strong>same</strong> App Group id in four places, plus a Team ID that actually owns that group.</p><table tabindex="0"><thead><tr><th>Place</th><th>What to set</th></tr></thead><tbody><tr><td><a href="https://developer.apple.com/account/resources/identifiers/list" target="_blank" rel="noreferrer">Apple Developer → Identifiers</a></td><td>App Group + both App IDs with <strong>App Groups</strong> capability</td></tr><tr><td>Xcode <strong>Signing &amp; Capabilities</strong> (app + widget targets)</td><td>Same group checked</td></tr><tr><td><code>plugins.widgets.appGroup</code> in <code>tauri.conf.json</code></td><td>Same string</td></tr><tr><td>JS <code>setWidgetConfig(config, group, …)</code></td><td>Same string as <code>group</code></td></tr></tbody></table><p>Swift <code>TauriWidgetProvider(appGroup:widgetId:)</code> must use that group too (CLI templates fill it).</p><h3 id="register-on-the-developer-portal" tabindex="-1">Register on the Developer portal <a class="header-anchor" href="#register-on-the-developer-portal" aria-label="Permalink to &quot;Register on the Developer portal&quot;">​</a></h3><p>You need <strong>Account Holder</strong> or <strong>Admin</strong> on the team that signs the app (the Team ID in Xcode / <code>bundle.iOS.developmentTeam</code>).</p><ol><li><strong>Identifiers → + → App Groups</strong><br> Identifier like <code>group.com.example.myapp</code> (reverse-DNS). Register.</li><li><strong>Identifiers → + → App IDs</strong> (type App) for the <strong>main</strong> bundle, e.g. <code>com.example.myapp</code><br> Enable <strong>App Groups</strong> → Configure → tick your group → Save.</li><li>Repeat for the <strong>widget</strong> bundle, e.g. <code>com.example.myapp.WidgetExtension</code><br> Same App Groups tick → Save.</li><li>In Xcode, set <strong>Team</strong> on both targets, Automatic signing, and add the App Groups capability if it is not already there.</li></ol><p>Then pin config:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;plugins&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;widgets&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;appGroup&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;group.com.example.myapp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;transport&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;appGroup&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bundle&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;iOS&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;developmentTeam&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;YOUR_TEAM_ID&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>iOS always needs <code>transport: &quot;appGroup&quot;</code>. macOS with a real Team ID should use <code>appGroup</code> too; for local ad-hoc without a shared container use <code>widgetContainer</code> (<a href="/tauri-plugin-widgets/guide/setup/macos">macOS setup</a>).</p><h3 id="identifier-is-not-available" tabindex="-1">“Identifier is not available” <a class="header-anchor" href="#identifier-is-not-available" aria-label="Permalink to &quot;“Identifier is not available”&quot;">​</a></h3><p>If the portal says <em>An Application Group with Identifier &#39;…&#39; is not available</em>:</p><ul><li>The id is <strong>already registered on another team</strong> (or another Apple ID). You cannot recreate it under this team.</li><li>Either switch Xcode / <code>developmentTeam</code> to the team that owns the group, <strong>or</strong> register a <strong>new</strong> unique id (e.g. <code>group.com.example.myapp2</code>) and update conf, entitlements, Swift, and JS together.</li></ul><p>Confirm you are editing Identifiers for the <strong>same</strong> team as <code>DEVELOPMENT_TEAM</code> / <code>bundle.iOS.developmentTeam</code>.</p><h3 id="signing-cheat-sheet" tabindex="-1">Signing cheat sheet <a class="header-anchor" href="#signing-cheat-sheet" aria-label="Permalink to &quot;Signing cheat sheet&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Host</th><th>Signing</th><th><code>transport</code></th></tr></thead><tbody><tr><td>iOS device / TestFlight</td><td>Apple Development or Distribution + Team ID</td><td><code>appGroup</code></td></tr><tr><td>iOS Simulator</td><td>Development / Automatic</td><td><code>appGroup</code> (portal group still required for Automatic profiles with the entitlement)</td></tr><tr><td>macOS, Team ID / Developer ID / MAS</td><td>Real identity</td><td><code>appGroup</code></td></tr><tr><td>macOS, ad-hoc (<code>-</code>)</td><td>No shared App Group container</td><td><code>widgetContainer</code></td></tr></tbody></table><p>List identities (or use the CLI):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tauri-widgets</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> signing</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">security</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> find-identity</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> codesigning</span></span></code></pre></div><p>For CI / local env vars used by Tauri Apple builds (<code>APPLE_DEVELOPMENT_TEAM</code>, optional ASC API key), keep secrets out of git. Unset <code>APPLE_API_KEY</code> / <code>APPLE_API_ISSUER</code> / <code>APPLE_API_KEY_PATH</code> when running <code>tauri ios build</code> if you need real codesign — with those set, tauri-cli may stamp a dummy identity.</p><h3 id="sanity-check" tabindex="-1">Sanity check <a class="header-anchor" href="#sanity-check" aria-label="Permalink to &quot;Sanity check&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tauri-widgets</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or: pnpm tauri-widgets doctor /path/to/your-app</span></span></code></pre></div><p>Checks conf ↔ entitlements ↔ project mentions of the App Group, and (when a built <code>.app</code> exists) ad-hoc vs Team ID mismatches. Details: <a href="/tauri-plugin-widgets/guide/doctor">Doctor</a>.</p><p>Platform walkthroughs: <a href="/tauri-plugin-widgets/guide/setup/ios">iOS setup</a> · <a href="/tauri-plugin-widgets/guide/setup/macos">macOS setup</a>.</p>`,35)])])}const k=s(o,[["render",p]]);export{u as __pageData,k as default};
