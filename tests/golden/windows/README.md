# Windows PNG goldens

Record via PreviewHost on a Windows VM (AdaptiveCards.Rendering.WinUI3), not the removed Mac SVG compositor:

```bash
just record-windows weather.small
```

JSON transpile snapshots live in `tests/snapshots/adaptive/`.
